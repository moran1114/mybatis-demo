<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.course.persistence.mapper.StaffProfileMapper">

    <resultMap id="staffProfileMap" type="dev.course.persistence.entity.StaffProfile">
        <id property="id" column="staff_id"/>
        <result property="name" column="full_name"/>
        <result property="gender" column="gender_mark"/>
        <result property="unitName" column="business_unit"/>
        <result property="positionTitle" column="position_title"/>
        <result property="monthlyPay" column="monthly_pay"/>
        <result property="entryDate" column="entry_date"/>
        <result property="activeState" column="active_state"/>
    </resultMap>

    <sql id="profileColumns">
        staff_id, full_name, gender_mark, business_unit,
        position_title, monthly_pay, entry_date, active_state
    </sql>

    <select id="selectAllProfiles" resultMap="staffProfileMap">
        SELECT <include refid="profileColumns"/>
        FROM staff_profile
        ORDER BY staff_id
    </select>

    <select id="selectProfileById" resultMap="staffProfileMap">
        SELECT <include refid="profileColumns"/>
        FROM staff_profile
        WHERE staff_id = #{id}
    </select>

    <insert id="insertProfile" useGeneratedKeys="true"
            keyProperty="id" keyColumn="staff_id">
        INSERT INTO staff_profile
            (full_name, gender_mark, business_unit, position_title,
             monthly_pay, entry_date, active_state)
        VALUES
            (#{name}, #{gender}, #{unitName}, #{positionTitle},
             #{monthlyPay}, #{entryDate}, #{activeState})
    </insert>

    <update id="updateProfile">
        UPDATE staff_profile
        SET full_name = #{name},
            gender_mark = #{gender},
            business_unit = #{unitName},
            position_title = #{positionTitle},
            monthly_pay = #{monthlyPay},
            entry_date = #{entryDate},
            active_state = #{activeState}
        WHERE staff_id = #{id}
    </update>

    <delete id="deleteProfileById">
        DELETE FROM staff_profile
        WHERE staff_id = #{id}
    </delete>

    <select id="selectBySearch" resultMap="staffProfileMap">
        SELECT <include refid="profileColumns"/>
        FROM staff_profile
        <where>
            <if test="search != null and search.nameKeyword != null and search.nameKeyword != ''">
                AND full_name LIKE CONCAT('%', #{search.nameKeyword}, '%')
            </if>
            <if test="search != null and search.gender != null and search.gender != ''">
                AND gender_mark = #{search.gender}
            </if>
            <if test="search != null and search.unitName != null and search.unitName != ''">
                AND business_unit = #{search.unitName}
            </if>
            <if test="search != null and search.activeState != null">
                AND active_state = #{search.activeState}
            </if>
            <if test="search != null and search.minimumPay != null">
                AND monthly_pay &gt;= #{search.minimumPay}
            </if>
        </where>
        ORDER BY monthly_pay DESC, staff_id
    </select>

    <select id="selectBySearchWithTrim" resultMap="staffProfileMap">
        SELECT <include refid="profileColumns"/>
        FROM staff_profile
        <trim prefix="WHERE" prefixOverrides="AND |OR ">
            <if test="search != null and search.nameKeyword != null and search.nameKeyword != ''">
                AND full_name LIKE CONCAT('%', #{search.nameKeyword}, '%')
            </if>
            <if test="search != null and search.gender != null and search.gender != ''">
                AND gender_mark = #{search.gender}
            </if>
            <if test="search != null and search.unitName != null and search.unitName != ''">
                AND business_unit = #{search.unitName}
            </if>
            <if test="search != null and search.activeState != null">
                AND active_state = #{search.activeState}
            </if>
            <if test="search != null and search.minimumPay != null">
                AND monthly_pay &gt;= #{search.minimumPay}
            </if>
        </trim>
        ORDER BY monthly_pay DESC, staff_id
    </select>

    <update id="updatePartial">
        UPDATE staff_profile
        <set>
            <if test="name != null">full_name = #{name},</if>
            <if test="gender != null">gender_mark = #{gender},</if>
            <if test="unitName != null">business_unit = #{unitName},</if>
            <if test="positionTitle != null">position_title = #{positionTitle},</if>
            <if test="monthlyPay != null">monthly_pay = #{monthlyPay},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="activeState != null">active_state = #{activeState},</if>
        </set>
        WHERE staff_id = #{id}
    </update>

    <insert id="insertBatch">
        INSERT INTO staff_profile
            (full_name, gender_mark, business_unit, position_title,
             monthly_pay, entry_date, active_state)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.name}, #{item.gender}, #{item.unitName}, #{item.positionTitle},
             #{item.monthlyPay}, #{item.entryDate}, #{item.activeState})
        </foreach>
    </insert>

    <delete id="deleteBatch">
        DELETE FROM staff_profile
        WHERE staff_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectPreferred" resultMap="staffProfileMap">
        SELECT <include refid="profileColumns"/>
        FROM staff_profile
        <where>
            <choose>
                <when test="exactName != null and exactName != ''">
                    full_name = #{exactName}
                </when>
                <when test="unitName != null and unitName != ''">
                    business_unit = #{unitName}
                </when>
                <otherwise>
                    1 = 1
                </otherwise>
            </choose>
        </where>
        ORDER BY staff_id
    </select>
</mapper>
